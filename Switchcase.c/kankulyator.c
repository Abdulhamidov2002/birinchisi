#include <stdio.h>
int main(){
    int a,b,natija;
    char c;
printf("son kiriting");
scanf("%d",a);
printf(" Mantiqiy amal kiriting");
scanf(" %c",&c);
printf("son kiriting");
scanf("%d",&b);

switch(c){
   
    case ('+'):
      natija=a+b;
      printf ("%d",natija);
      break;
    case ('+'):
      natija=a+b;
      printf ("%d",natija);
      break;
    case ('+'):
      natija=a+b;
      printf ("%d",natija);
      break; 
    
}
}